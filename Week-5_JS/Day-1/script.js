var name = 'John';
var num = 10;
var flag = true;

let a;
a = null;

const color = "#ff3rdf";

// comments
/*
dasdd
*/
a = 4;

/** keywords
 * @author sasha
 */
console.log('hello');
console.error('error');
let str = `
    js
    node
    `;
console.log(str);
let str2 = "node" + "js"
console.log(str2)
let d = '3';
let z = 4;
console.log(Number(d) + z);
let b = 3, g = b, u = b;
const tree = g++;

console.log(tree);

// To sum it up, for practical applications, we can use the OR operator to set default values, 
// and we can use the AND operator to execute code in the second operand if the first one is true.
